#From the shell of a debian container
#install everything you need to compile C source code and push it onto a git repo
#For this exercise, you should only specify the commands to be run directly in the container

apt-get update
apt-get upgrade -y
apt-get install -y build-essential git

#Start Debian container: docker run -it --rm debian (as in ex08)
#man apt-get: https://linux.die.net/man/8/apt-get
#flag -y, says yes to all the prompts
#build-essential contains info important for building Debian packages including gcc compiler
